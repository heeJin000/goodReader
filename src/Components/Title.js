import React, {Component} from 'react';

class Title extends Component{
    render(){
      return(
        <header>
          <h1>GoodReader</h1>     
        </header>
      );
    }
  }

export default Title;